import React from 'react';
import Calendar from '../components/Calendar';

const Home = () => {
    return (
        <section className="section-home">
            <Calendar />
        </section>
    );
};

export default Home;
