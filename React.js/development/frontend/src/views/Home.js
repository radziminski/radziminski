import React from 'react';
import Calendar from '../components/Calendar';

const Home = () => {
    return (
        <section className="home-view">
            <Calendar />
        </section>
    );
};

export default Home;
